# Referências Científicas

> Este arquivo substitui `TESSERA Reducing Failures in Workflow Memory`, que
> existia vazio na pasta `DOCS/` original — provavelmente um placeholder
> criado para colar o conteúdo de um paper, nunca preenchido.

Os documentos do Tessera (`ARCHITECTURE.md`, `CODE_EXPLANATION.md`,
`PROCEDURAL_ANCHORS.md`) citam papers do estado da arte usando marcadores
numéricos `[1]`, `[2]`, `[3]`, `[6]`, `[8]`, `[10]`, `[12]`, `[13]`. As
referências completas (título, autores, link) ainda não foram registradas
neste repositório.

**Ação pendente**: se você tiver as referências originais (por exemplo, o
paper que dá nome ao arquivo vazio — algo relacionado a "reducing failures
in workflow memory" — e os demais citados como QUMem, MemORAI, FinPerMA),
cole aqui a lista completa numerada para que as citações no restante da
documentação passem a apontar para fontes verificáveis.

Placeholder de estrutura sugerida:

```
[1] <título>, <autores>, <ano>. <link>
[2] <título>, <autores>, <ano>. <link>
[3] QUMem: ...
[6] MemORAI: ...
[8] ...
[10] ...
[12] State Contamination / Memory Laundering: ...
[13] FinPerMA: ...
```
